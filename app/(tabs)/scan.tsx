import { Text, View } from 'react-native';

export default function ScanScreen() {
  return (
    <View className="flex-1 items-center justify-center bg-[#FAFAF8]">
      <Text className="text-[#2C2C2A] text-lg font-semibold">Scan</Text>
    </View>
  );
}
